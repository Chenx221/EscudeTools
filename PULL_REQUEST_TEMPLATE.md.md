## 概述

首先非常感谢 @Chenx221 开发了 EscudeTools 这个实用工具。  
我使用 Inno Setup 对它进行了打包，生成了 Windows 下的标准安装程序，方便不熟悉命令行的用户直接安装使用。

## 更改内容

- 新增 Inno Setup 打包脚本（`.iss` 文件）
- 生成 Windows 安装程序（`EscudeTools-v1.0.0-win-setup.exe`）
- 安装包特点：
  - 支持选择安装目录
  - 自动将程序路径添加到系统 PATH 环境变量（可选）
  - 附带 HTML 格式的使用说明文档
  - 支持创建桌面快捷方式和开始菜单入口

## 为什么需要这个打包？

- 降低使用门槛：用户无需手动配置 .NET 运行环境或复制文件
- 符合 Windows 软件安装习惯：提供标准的安装/卸载流程
- 便于分发和版本管理

## 合并建议

如果合适，希望能将这些打包相关文件（主要是 `.iss` 脚本和说明文档模板）合并到 `main` 分支，以便后续维护和自动构建。  
安装程序本身也可以放入 Releases 页面供用户下载。

## 测试情况

- 在 Windows 10/11 上测试安装、卸载、PATH 添加功能均正常
- 安装后可在任意目录下直接调用 `EscudeTools.exe`（需用户重新打开命令行）

## 附加说明

- 打包脚本位于 `installer/` 目录（若需要，我可按项目结构调整）
- 安装程序不包含任何额外广告或修改系统非必要项

感谢审阅，期待您的反馈！